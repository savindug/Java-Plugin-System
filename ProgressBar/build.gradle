plugins {
    id 'java'
}

version 'unspecified'

repositories {
    jcenter()
}

dependencies {
    //testCompile group: 'junit', name: 'junit', version: '4.12'
    implementation project(':Plugin_API')
}
